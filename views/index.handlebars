<h1>Burgers!</h1>

<h2>Burgers that are not launched!</h2>

<ul>
  {{#each burgers}}
    {{#unless launched}}
      {{> burgers/burger-block launch=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are launched!</h2>

<ul>
  {{#each burgers}}
    {{#if launched}}
      {{> burgers/burger-block launch=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="launched">Launched?</label><br>
    <input type="radio" name="launched" value="1" checked> Launched!<br>
    <input type="radio" name="launched" value="0"> Ready for Launch
  </div>

  <button type="submit">Add Burger</button>
</form>
